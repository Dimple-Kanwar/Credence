{
  "name": "credence-mcp-credit-bureau",
  "version": "0.1.0",
  "private": true,
  "description": "MCP server exposing the Credence on-chain credit bureau (ENSv2 + World + The Graph + Hedera) to AI agents in natural language.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check test/smoke.js",
    "smoke": "node test/smoke.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "dotenv": "^16.4.5",
    "zod": "^3.25.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}